import { TestPaperQuestion } from './test-paper-question.model';

describe('TestPaperQuestion', () => {
  it('should create an instance', () => {
    expect(new TestPaperQuestion()).toBeTruthy();
  });
});
